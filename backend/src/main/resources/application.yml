spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: ****
    password: ****
    properties.mail.smtp.auth: true
    properties.mail.smtp.starttls.enable: true

  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect

    hibernate:
      ddl-auto: update

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/dabs?createDatabaseIfNotExist=tru
    username: ****
    password: ****

server:
  port: 8083
